# English Study App

一个帮助你高效学习英语单词的移动应用。

## 技术栈

### 前端 (client)
- **uni-app** + **Vue 3** + **TypeScript**
- **Vite** 构建工具
- 支持 iOS、Android、H5、小程序多端

### 后端 (server)
- **Node.js** + **Express** + **TypeScript**
- **MongoDB** 数据库
- **JWT** 用户认证
- **有道 API** 词典服务

## 功能特性

- 📚 **预设词库** - CET4/CET6/雅思/托福等标准词库
- 🔍 **搜索添加** - 通过有道 API 搜索任意单词
- 🔊 **发音功能** - 美音/英音标准发音
- 📖 **音标显示** - 完整的音标信息
- 📝 **例句展示** - 帮助理解单词用法
- 🔄 **智能复习** - 基于艾宾浩斯遗忘曲线
- 📊 **学习统计** - 追踪学习进度
- ☁️ **云端同步** - 多设备数据同步

## 快速开始

### 1. 配置后端

```bash
cd server

# 复制环境变量配置
cp .env.example .env

# 编辑 .env 文件，填写配置
# - MongoDB 连接地址
# - JWT 密钥
# - 有道 API Key (https://ai.youdao.com/ 申请)

# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

### 2. 配置前端

```bash
cd client

# 安装依赖
pnpm install

# 修改 src/api/index.ts 中的 BASE_URL 为你的后端地址

# 启动 H5 开发服务器
pnpm dev:h5

# 或打包 App
pnpm build:app
```

### 3. 配置有道 API

1. 访问 [有道智云](https://ai.youdao.com/)
2. 注册并创建应用
3. 获取 App Key 和 App Secret
4. 填入 `server/.env` 文件

## 项目结构

```
EnglishStudy/
├── client/                 # uni-app 前端
│   ├── src/
│   │   ├── api/           # API 封装
│   │   ├── components/    # 公共组件
│   │   │   └── WordCard.vue  # 单词卡片组件
│   │   ├── pages/         # 页面
│   │   │   ├── login/     # 登录注册
│   │   │   ├── home/      # 首页
│   │   │   ├── study/     # 学习页面
│   │   │   ├── search/    # 搜索单词
│   │   │   ├── wordbook/  # 词库管理
│   │   │   └── profile/   # 个人中心
│   │   └── pages.json     # 页面配置
│   └── package.json
│
└── server/                 # Node.js 后端
    ├── src/
    │   ├── config/        # 配置
    │   ├── models/        # 数据模型
    │   ├── routes/        # API 路由
    │   ├── services/      # 服务层
    │   │   └── youdao.ts  # 有道 API 封装
    │   ├── middleware/    # 中间件
    │   └── index.ts       # 入口文件
    ├── .env.example       # 环境变量示例
    └── package.json

```

## API 接口

### 用户认证
- `POST /api/auth/register` - 注册
- `POST /api/auth/login` - 登录
- `GET /api/auth/profile` - 获取用户信息

### 单词
- `GET /api/word/search?word=xxx` - 搜索单词
- `POST /api/word/add` - 添加到词库
- `GET /api/word/progress` - 获取学习进度
- `POST /api/word/record` - 更新学习记录
- `GET /api/word/review` - 获取待复习单词

### 词库
- `GET /api/wordbook/preset` - 获取预设词库
- `POST /api/wordbook/subscribe/:id` - 订阅词库
- `GET /api/wordbook/user/subscribed` - 获取已订阅词库

## License

MIT
